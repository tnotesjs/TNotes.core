# Frozen VitePress security audit — 2026-09-05

Command: `pnpm audit:vitepress` from the Core repository.

Result: failed with 43 known production dependency advisories:

- 1 critical
- 13 high
- 24 moderate
- 5 low

The critical advisory is `GHSA-hmx5-qpq5-p643` in the frozen Swiper line. High
advisories include the frozen Mermaid/Lodash and MathJax/XML dependency trees.
This is an intentionally immutable recovery image, not the active publishing
path. It must only build trusted knowledge-base source in an isolated container;
it must not be exposed as a long-running public service.

The active path is `@tnotesjs/ssg`. Any request to revive this baseline must
first re-run the audit and explicitly accept or patch the current report.
